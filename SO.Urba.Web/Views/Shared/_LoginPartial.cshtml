@using SO.Urba.Managers

@if (Request.IsAuthenticated)
{      
   <ul class="nav nav-pills pull-right">
        <li class="dropdown">
            <a href="" class="dropdown-toggle" data-toggle="dropdown">
                @CurrentMember.member.contactInfo.firstName @{if (CurrentMember.hasRoleOf("Admin")) {<span>(Admin)</span> } else {<span>(User)</span> }}
                <b class="caret white-caret"></b></a>

            <ul class="dropdown-menu">      
                <li>@Html.ActionLink("Profile", "Profile", "Account")</li>
                <li class="divider"></li>
                <li >@Html.ActionLink("Logout", "Logout", "Account")</li>  
            </ul>
        </li>
    </ul>
}
 
